<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.support.webapp_to_webview">

    <!-- Permiso para navegar por Internet (ya lo tenías) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- ✨ NUEVO: Permiso para guardar archivos en Android 9 e inferiores -->
    <!-- android:maxSdkVersion="28" significa que solo se pide en Android 9 o menor -->
    <!-- En Android 10+ esto se ignora automáticamente -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Webapptowebview"
        android:usesCleartextTraffic="true">  <!-- ✨ NUEVO: Permite HTTP si es necesario -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
